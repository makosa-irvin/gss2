'use client';

import { useState } from 'react';
import { CheckCircle2, MapPin } from 'lucide-react';
import { DestinationCard } from './CatalogCards';
import type { Destination } from '../lib/types';

export function DestinationExplorer({destinations}:{destinations:Destination[]}){const[selected,setSelected]=useState('all');const filtered=destinations.filter(d=>selected==='all'||d.country.toLowerCase()===selected.toLowerCase());const filters=[['All destinations','all'],['Kenya','Kenya'],['Tanzania','Tanzania'],['Zanzibar & Coast','Zanzibar']] as const;return <div className="max-w-7xl mx-auto px-4 sm:px-8 py-10 space-y-9"><section className="rounded-3xl border border-white/10 bg-gradient-to-br from-[#142019] to-shell p-7 sm:p-10 text-center shadow-xl"><div className="max-w-3xl mx-auto space-y-4"><div className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-widest text-brand-soft"><MapPin className="w-4 h-4"/>Where to go</div><h1 className="font-serif-luxury text-3xl sm:text-5xl font-bold text-white leading-tight">Choose the landscapes that shape your safari.</h1><p className="text-base text-on-shell-muted leading-relaxed">From Kenya’s wildlife-rich conservancies to Tanzania’s vast plains and the Indian Ocean coast, discover what makes each place worth adding to your route.</p><div className="flex flex-wrap justify-center gap-x-5 gap-y-2 text-sm font-semibold text-on-shell"><span className="inline-flex items-center gap-1.5"><CheckCircle2 className="w-4 h-4 text-brand-soft"/>Wildlife guidance</span><span className="inline-flex items-center gap-1.5"><CheckCircle2 className="w-4 h-4 text-brand-soft"/>Best travel seasons</span><span className="inline-flex items-center gap-1.5"><CheckCircle2 className="w-4 h-4 text-brand-soft"/>Matching safari ideas</span></div></div></section><div className="flex flex-wrap items-center justify-center gap-2">{filters.map(([label,value])=><button key={value} onClick={()=>setSelected(value)} className={`min-h-11 px-5 rounded-xl text-sm font-bold transition-all ${selected===value?'bg-brand-strong text-white shadow-md':'bg-white text-ink-muted border border-border-strong hover:border-brand hover:text-ink-strong'}`}>{label}</button>)}</div><p className="text-sm text-on-shell-muted text-center">Showing <span className="font-bold text-white">{filtered.length}</span> destinations</p><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">{filtered.map(d=><DestinationCard key={d.id} destination={d}/>)}</div></div>}
