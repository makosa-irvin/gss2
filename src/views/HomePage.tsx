import React, { Suspense } from 'react';
import { useData } from '../context/DataContext';
import { Tour, Destination, Hotel } from '../types';
import { PageMeta } from '../components/common/PageMeta';
import { unsplashCardImage } from '../lib/imageUrl';
import { SCHEMA_CONTEXT, SITE_URL } from '../lib/site';
import { SafariFinderBar } from '../components/search/SafariFinderBar';
import { TourCard } from '../components/tours/TourCard';
import { DestinationCard } from '../components/destinations/DestinationCard';
import { HotelCard } from '../components/hotels/HotelCard';
import { VerifiedReviewsSection } from '../components/reviews/VerifiedReviewsSection';
import { ArrowRight, Binoculars, CheckCircle2, Compass, Heart, MessageCircle, Palmtree, ShieldCheck, Sparkles, Users } from 'lucide-react';

const DeferredSafariBuilder = React.lazy(() => import('../components/builder/SafariBuilderWizard').then(m => ({ default: m.SafariBuilderWizard })));

interface HomePageProps {
  onNavigate: (view: string, payload?: any) => void;
  onSelectTour: (tour: Tour) => void;
  onSelectDestination: (dest: Destination) => void;
  onSelectHotel: (hotel: Hotel) => void;
  onOpenEnquiryModal: (payload?: any) => void;
}

const styles = [
  ['Big 5', Binoculars, 'Classic private game drives built around East Africa’s iconic wildlife.'],
  ['Great Migration', Compass, 'Mara and Serengeti timing for one of Africa’s great wildlife movements.'],
  ['Family', Users, 'Flexible pacing, practical logistics and family-friendly stays.'],
  ['Honeymoon', Heart, 'Private wildlife time, intimate camps and beach combinations.'],
  ['Luxury', Sparkles, 'Exceptional lodges, private conservancies and fly-in possibilities.'],
  ['Safari & Beach', Palmtree, 'Combine the bush with Diani, Watamu or Zanzibar.']
] as const;

export const HomePage: React.FC<HomePageProps> = ({ onNavigate, onSelectTour, onSelectDestination, onSelectHotel, onOpenEnquiryModal }) => {
  const { tours, destinations, hotels, blogPosts, settings, setIsKenyanResidentMode, getWhatsAppUrl } = useData();
  const featured = tours.filter(t => t.featured || t.popular);
  const featuredTours = (featured.length ? featured : tours).slice(0, 6);

  return (
    <div className="pb-20 space-y-20 sm:space-y-28">
      <PageMeta title="Good Secrets Safaris" description="Private Kenya, Tanzania and Zanzibar safaris planned with an East Africa-based team. Explore itinerary ideas, independent reviews and tailor-made safari planning." canonicalPath="/" image="/images/catalog/vehicle-next-to-the-great-migration.jpg" structuredData={{ '@context': SCHEMA_CONTEXT, '@type': 'TravelAgency', name: settings.companyName, url: SITE_URL, description: settings.description, email: settings.contact.email, telephone: settings.contact.phone, address: settings.contact.address, areaServed: ['Kenya', 'Tanzania', 'Zanzibar'], sameAs: Object.values(settings.social || {}).filter(Boolean) }} />

      <section className="relative min-h-[84vh] flex flex-col justify-end overflow-hidden border-b border-white/10">
        <img src="/images/catalog/vehicle-next-to-the-great-migration.jpg" alt="Safari vehicle watching the Great Migration in East Africa" fetchPriority="high" className="absolute inset-0 h-full w-full object-cover object-center" />
        <div className="absolute inset-0 bg-gradient-to-t from-shell via-shell/60 to-black/20" /><div className="absolute inset-0 bg-gradient-to-r from-black/55 via-transparent to-black/10" />
        <div className="relative max-w-7xl w-full mx-auto px-4 sm:px-8 pt-20 pb-8 sm:pb-12">
          <div className="max-w-4xl space-y-6">
            <span className="inline-flex items-center gap-2 rounded-full bg-black/45 backdrop-blur-md px-4 py-2 border border-white/25 text-brand-soft text-xs font-bold uppercase tracking-widest"><Compass className="w-4 h-4" />Private safaris · Kenya · Tanzania · Zanzibar</span>
            <h1 className="font-serif-luxury text-5xl sm:text-7xl lg:text-8xl font-bold text-white leading-[0.98] tracking-tight">Your safari. Our home.</h1>
            <p className="text-base sm:text-xl text-white/95 max-w-2xl font-medium leading-relaxed">Private 4x4 journeys, experienced local guides and carefully chosen stays — shaped around your dates, pace and priorities.</p>
            <div className="flex flex-col sm:flex-row gap-3 max-w-3xl"><button onClick={() => onNavigate('builder')} className="min-h-12 px-7 rounded-xl bg-brand-soft hover:bg-brand-soft text-ink-strong font-extrabold text-sm shadow-xl">Build My Safari</button><button onClick={() => onNavigate('tours')} className="min-h-12 px-7 rounded-xl border border-white/30 bg-black/25 hover:bg-black/40 text-white font-bold text-sm backdrop-blur-sm">Explore Safari Ideas</button><button onClick={() => onNavigate('plan-with-us')} className="min-h-12 px-5 text-white/95 hover:text-brand-soft font-bold text-sm">How we plan your safari →</button></div>
            <div className="flex flex-wrap gap-x-5 gap-y-2 text-xs sm:text-sm font-semibold text-white/95"><span className="inline-flex items-center gap-1.5"><CheckCircle2 className="w-4 h-4 text-brand-soft" />No payment to enquire</span><span className="inline-flex items-center gap-1.5"><CheckCircle2 className="w-4 h-4 text-brand-soft" />Private 4x4</span><span className="inline-flex items-center gap-1.5"><CheckCircle2 className="w-4 h-4 text-brand-soft" />Flexible dates & pacing</span></div>
          </div>
          <div className="mt-10"><SafariFinderBar onSearch={(filters) => onNavigate('tours', filters)} /></div>
        </div>
      </section>

      <VerifiedReviewsSection />

      <section className="max-w-7xl mx-auto px-4 sm:px-8 grid lg:grid-cols-12 gap-8 lg:gap-14 items-center">
        <div className="lg:col-span-5 space-y-5"><span className="text-xs font-bold uppercase tracking-widest text-brand-soft">Personal planning, on the ground</span><h2 className="font-serif-luxury text-3xl sm:text-5xl font-bold text-white leading-tight">A safari is too important to feel anonymous.</h2><p className="text-base text-on-shell-muted leading-relaxed">You should understand who is shaping your route, what is included and how to get help while you travel. We plan private East Africa journeys around real people rather than treating an itinerary as a checkout product.</p><div className="grid sm:grid-cols-2 gap-3">{[['1','Tell us your priorities'],['2','Refine route & stays'],['3','Review before committing'],['4','Travel with local support']].map(([n,label]) => <div key={n} className="border-t border-white/15 pt-3"><span className="font-serif-luxury text-xl text-brand-soft">{n}</span><strong className="text-sm text-white ml-3">{label}</strong></div>)}</div><button onClick={() => onNavigate('plan-with-us')} className="min-h-11 inline-flex items-center gap-2 text-brand-soft hover:text-white font-bold text-sm">See how we plan your safari <ArrowRight className="w-4 h-4" /></button></div>
        <div className="lg:col-span-7 grid grid-cols-5 gap-3 sm:gap-4"><div className="col-span-3 row-span-2 rounded-[2rem] overflow-hidden min-h-80 border border-white/10"><img src="/images/catalog/family-safari-game-drive.jpg" alt="Travelers enjoying a private safari game drive" loading="lazy" className="w-full h-full object-cover" /></div><div className="col-span-2 rounded-[2rem] overflow-hidden min-h-40 border border-white/10"><img src="/images/catalog/picnic-lunch-in-the-wild.jpg" alt="Safari picnic lunch in the wild" loading="lazy" className="w-full h-full object-cover" /></div><div className="col-span-2 rounded-[2rem] overflow-hidden min-h-40 border border-white/10"><img src="/images/catalog/honey-moon-in-samburu.jpg" alt="Couple enjoying a Samburu safari" loading="lazy" className="w-full h-full object-cover" /></div></div>
      </section>

      <section className="cv-auto max-w-7xl mx-auto px-4 sm:px-8 space-y-8"><div className="flex flex-col md:flex-row md:items-end justify-between gap-4"><div><span className="text-xs font-bold uppercase tracking-widest text-brand-soft">Safari ideas</span><h2 className="font-serif-luxury text-3xl sm:text-4xl font-bold text-white mt-1">Start with a proven route. Make it yours.</h2><p className="text-base text-on-shell-muted mt-2 max-w-2xl">Compare duration, country and guide price, then adjust dates, accommodation and pacing with the safari team.</p></div><button onClick={() => onNavigate('tours')} className="inline-flex min-h-11 items-center gap-2 text-sm font-bold text-brand-soft hover:text-white">View all {tours.length} safari ideas <ArrowRight className="w-4 h-4" /></button></div><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">{featuredTours.map(tour => <TourCard key={tour.id} tour={tour} onSelect={onSelectTour} onEnquire={onOpenEnquiryModal} />)}</div></section>

      <section className="cv-auto max-w-7xl mx-auto px-4 sm:px-8"><div className="rounded-[2rem] bg-page border border-border-strong p-7 sm:p-10 lg:p-12 text-ink-strong"><div className="grid lg:grid-cols-12 gap-10"><div className="lg:col-span-4"><span className="text-xs font-extrabold uppercase tracking-widest text-brand-deep">Choose the feeling first</span><h2 className="font-serif-luxury text-3xl sm:text-4xl font-bold mt-2">What kind of safari are you imagining?</h2><p className="text-sm text-ink-muted mt-3 leading-relaxed">Use these as starting points rather than boxes you have to fit into.</p></div><div className="lg:col-span-8 grid sm:grid-cols-2 gap-x-8">{styles.map(([title,Icon,desc]) => <button key={title} onClick={() => onNavigate('tours',{travelStyle:title})} className="group text-left py-5 border-b border-border-strong flex gap-4"><span className="w-11 h-11 shrink-0 rounded-full border border-border-strong flex items-center justify-center text-brand-deep group-hover:bg-action group-hover:text-white transition-colors"><Icon className="w-5 h-5" /></span><span><strong className="font-serif-luxury text-xl block group-hover:text-brand-deep">{title}</strong><span className="text-sm text-ink-muted mt-1 block leading-relaxed">{desc}</span></span></button>)}</div></div></div></section>

      <section className="cv-auto max-w-7xl mx-auto px-4 sm:px-8 space-y-8"><div className="flex flex-col md:flex-row md:items-end justify-between gap-4"><div><span className="text-xs font-bold uppercase tracking-widest text-brand-soft">Destination guides</span><h2 className="font-serif-luxury text-3xl sm:text-4xl font-bold text-white mt-1">Learn the parks before choosing the route.</h2><p className="text-base text-on-shell-muted mt-2">Wildlife, seasons and route ideas across East Africa.</p></div><button onClick={() => onNavigate('destinations')} className="inline-flex min-h-11 items-center gap-2 text-sm font-bold text-brand-soft hover:text-white">All destinations <ArrowRight className="w-4 h-4" /></button></div><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">{destinations.slice(0,6).map(dest => <DestinationCard key={dest.id} destination={dest} onSelect={onSelectDestination} />)}</div></section>

      <section className="cv-auto max-w-7xl mx-auto px-4 sm:px-8 grid lg:grid-cols-12 gap-8 items-center"><div className="lg:col-span-6 rounded-[2rem] overflow-hidden aspect-[4/3] border border-white/10"><img src="/images/catalog/sundowner-at-amboseli.jpg" alt="Sundowner experience in Amboseli" loading="lazy" className="w-full h-full object-cover" /></div><div className="lg:col-span-6 space-y-5"><span className="text-xs font-bold uppercase tracking-widest text-brand-soft">Why Good Secrets</span><h2 className="font-serif-luxury text-3xl sm:text-4xl font-bold text-white">Local expertise is most useful when it changes the trip.</h2><div className="space-y-4">{[['Personal planning','Change the route, pace and accommodation around the people actually traveling.'],['Independent proof','Use independent traveler reviews to verify what past guests say.'],['Local communication','Email, phone and WhatsApp keep the East Africa team within reach.'],['Clarity before commitment','Review itinerary, inclusions and trip terms before deciding to proceed.']].map(([title,desc]) => <div key={title} className="flex gap-3"><ShieldCheck className="w-5 h-5 text-brand-soft shrink-0 mt-0.5" /><div><strong className="text-white">{title}</strong><p className="text-sm text-on-shell-muted mt-1">{desc}</p></div></div>)}</div><div className="flex flex-wrap gap-4"><button onClick={() => onNavigate('about')} className="min-h-11 inline-flex items-center gap-2 text-brand-soft font-bold text-sm">Meet Good Secrets <ArrowRight className="w-4 h-4" /></button><button onClick={() => onNavigate('reviews')} className="min-h-11 text-white font-bold text-sm">Traveler reviews →</button></div></div></section>

      {hotels.length > 0 && <section className="cv-auto max-w-7xl mx-auto px-4 sm:px-8 space-y-8"><div className="flex flex-col md:flex-row md:items-end justify-between gap-4"><div><div className="inline-flex items-center gap-2 rounded-full bg-action-soft px-3 py-1.5 text-xs font-bold text-action border border-action-border mb-2">Kenyan resident offers</div><h2 className="font-serif-luxury text-3xl sm:text-4xl font-bold text-white">Beach stays and safari extensions</h2><p className="text-base text-on-shell-muted mt-2">Diani, Watamu and Mombasa stays, with dedicated resident pricing where available.</p></div><button onClick={() => { setIsKenyanResidentMode(true); onNavigate('hotels'); }} className="inline-flex min-h-11 items-center gap-2 text-sm font-bold text-brand-soft hover:text-white">Browse stays <ArrowRight className="w-4 h-4" /></button></div><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">{hotels.slice(0,4).map(hotel => <HotelCard key={hotel.id} hotel={hotel} onSelect={onSelectHotel} onEnquire={onOpenEnquiryModal} />)}</div></section>}

      <section id="safari-builder-section" className="cv-auto max-w-7xl mx-auto px-4 sm:px-8 space-y-8"><div className="text-center max-w-2xl mx-auto space-y-2"><span className="text-xs font-bold uppercase tracking-widest text-brand-soft">Not sure where to start?</span><h2 className="font-serif-luxury text-3xl sm:text-4xl font-bold text-white">Build your safari preferences</h2><p className="text-base text-on-shell-muted">Answer a few questions to narrow the route. A human safari planner still helps shape the final itinerary.</p></div><Suspense fallback={<div className="min-h-52 rounded-3xl border border-white/10 bg-white/5 flex items-center justify-center text-sm text-on-shell-subtle">Loading safari planner…</div>}><DeferredSafariBuilder onSelectTour={onSelectTour} onCompleteEnquiry={onOpenEnquiryModal} /></Suspense></section>

      {blogPosts.length > 0 && <section className="cv-auto max-w-7xl mx-auto px-4 sm:px-8 space-y-8"><div className="flex flex-col md:flex-row md:items-end justify-between gap-4"><div><span className="text-xs font-bold uppercase tracking-widest text-brand-soft">Plan with confidence</span><h2 className="font-serif-luxury text-3xl sm:text-4xl font-bold text-white mt-1">Safari guides & practical advice</h2><p className="text-base text-on-shell-muted mt-2">Use our destination and planning content before you choose an itinerary.</p></div><button onClick={() => onNavigate('guides')} className="inline-flex min-h-11 items-center gap-2 text-sm font-bold text-brand-soft hover:text-white">Read planning guides <ArrowRight className="w-4 h-4" /></button></div><div className="grid grid-cols-1 md:grid-cols-3 gap-6">{blogPosts.slice(0,3).map(post => <button type="button" key={post.id} onClick={() => onNavigate('blog',{postSlug:post.slug})} className="group text-left rounded-2xl bg-white border border-border-strong overflow-hidden hover:border-brand-strong transition-all"><span className="aspect-[16/10] overflow-hidden bg-surface-muted block"><img src={unsplashCardImage(post.featuredImage,700)} alt={post.title} className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-[1.03]" loading="lazy" /></span><span className="p-5 block"><span className="text-xs text-brand-deep font-bold">{post.category} · <span className="text-ink-muted">{post.readingTime}</span></span><span className="font-serif-luxury text-xl font-bold text-ink-strong mt-2 block">{post.title}</span><span className="text-sm text-ink-muted mt-2 line-clamp-2 block">{post.excerpt}</span></span></button>)}</div></section>}

      <section className="cv-auto max-w-7xl mx-auto px-4 sm:px-8"><div className="relative overflow-hidden rounded-[2rem] border border-brand-deep bg-shell p-8 sm:p-12"><div className="grid lg:grid-cols-12 gap-8 items-center"><div className="lg:col-span-8"><span className="text-xs font-bold uppercase tracking-widest text-brand-soft">Start with a conversation</span><h2 className="font-serif-luxury text-3xl sm:text-4xl font-bold text-white mt-2">Tell us what would make the safari worth the journey.</h2><p className="text-base text-on-shell-muted mt-3 max-w-2xl">Share dates, group size and priorities. We can suggest a practical route without requiring payment to enquire.</p></div><div className="lg:col-span-4 flex flex-col gap-3"><button onClick={() => onOpenEnquiryModal()} className="min-h-12 rounded-xl bg-brand-soft hover:bg-brand-soft text-ink-strong font-extrabold text-sm">Request My Safari Plan</button><a href={getWhatsAppUrl({})} target="_blank" rel="noopener noreferrer" className="min-h-12 rounded-xl border border-white/30 bg-white/5 hover:bg-white/10 text-white font-bold text-sm inline-flex items-center justify-center gap-2"><MessageCircle className="w-4 h-4" />Ask on WhatsApp</a></div></div></div></section>
    </div>
  );
};
